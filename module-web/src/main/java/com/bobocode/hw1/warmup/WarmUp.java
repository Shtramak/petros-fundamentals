package com.bobocode.hw1.warmup;

import java.util.List;

public class WarmUp {

    /*
        Using one Stream API statement find a sum of all prime numbers from skipPosition to numberOfElements inclusively.
        A prime number is a natural number greater than 1 that has no positive divisors other than 1 and itself.
    */
    public static int sumOfPrimes(int skip, int numOfElements) {
        throw new UnsupportedOperationException();
    }

    /*
        Using one Stream API statement find a sum of the first sequence of positive numbers in the stream.
        A sequence of positive numbers is all numbers that appear one after another and have positive values. Any negative number breaks the sequence.
        Given the following stream:
        -4, -29, 5, 2, 3, -9, 12
        The result should be: 10
    */
    public static int sumOfPositiveSequenceFrom(List<Integer> integers) {
        throw new UnsupportedOperationException();
    }
}
